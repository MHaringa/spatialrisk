% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concentration.R
\name{radius_sum}
\alias{radius_sum}
\alias{concentration}
\title{Sum values within a radius around target coordinates}
\usage{
radius_sum(
  sub,
  full,
  value,
  lon_sub = "lon",
  lat_sub = "lat",
  lon_full = "lon",
  lat_full = "lat",
  radius = 200,
  display_progress = TRUE
)

concentration(
  sub,
  full,
  value,
  lon_sub = lon,
  lat_sub = lat,
  lon_full = lon,
  lat_full = lat,
  radius = 200,
  display_progress = TRUE
)
}
\arguments{
\item{sub}{A data.frame of target points for which sums are calculated.
Must include at least columns for longitude and latitude.}

\item{full}{A data.frame containing reference points. Must include at least
columns for longitude, latitude, and the value of interest to summarize.}

\item{value}{A string giving the name of the column in `full` to be summed.}

\item{lon_sub}{A string with the name of the longitude column in `sub`.
Default is `"lon"`.}

\item{lat_sub}{A string with the name of the latitude column in `sub`.
Default is `"lat"`.}

\item{lon_full}{A string with the name of the longitude column in `full`.
Default is `"lon"`.}

\item{lat_full}{A string with the name of the latitude column in `full`.
Default is `"lat"`.}

\item{radius}{Numeric. Radius of the circle in meters. Must be positive
(default: 200).}

\item{display_progress}{Logical. Whether to display a progress bar.
Default is `TRUE`.}
}
\value{
A data.frame equal to `sub` with an additional numeric column
  `radius_sum` containing the summed values from `full`.
}
\description{
Calculates the sum of all observations from a reference data set
that fall within a given radius (in meters) of each target point.

`r lifecycle::badge("deprecated")`

`concentration()` was renamed to [radius_sum()].
}
\details{
This function uses a C++ backend for efficient distance calculations
(Haversine formula).
}
\examples{
sub <- data.frame(location = c("p1", "p2"),
                  lon = c(6.561561, 6.561398),
                  lat = c(53.21369, 53.21326))

full <- data.frame(lon = c(6.5614, 6.5620, 6.5630),
                   lat = c(53.2132, 53.2140, 53.2150),
                   amount = c(10, 20, 15))

radius_sum(sub, full, value = "amount", radius = 100)

}
\author{
Martin Haringa
}
